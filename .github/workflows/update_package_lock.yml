jobs:
  update_package_lock:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os:
          - ubuntu-latest
        browser:
          - chrome
        node-version:
          - 20.9.0
    steps:
      - run: npm test
        env:
          BROWSER: ${{ matrix.browser }}

